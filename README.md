# HyPixelic/constants

## ✨ Quick Start

[`npm`](https://npmjs.com/) » `npx jsr add @hypixelic/constants`<br/>
[`pnpm`](https://pnpm.io/) » `pnpm dlx jsr add @hypixelic/constants`<br/>
[`bun`](https://bun.sh/) » `bunx jsr add @hypixelic/constants`

```TS
import { GUILD_XP } from "@hypixelic/constants"

const XpNeededPerGuildLevel: number[] = GUILD_XP
```

> [!NOTE]
> Credits to: https://github.com/SkyCryptWebsite/SkyCrypt and others.
